machine:
  node:
    version: 0.12.0
dependencies:
  pre:
    - sudo pip install awscli
deployment:
  production:
    branch: master
    commands:
      - aws s3 sync s3://backlog-to-chatwork/default.json config/default.json
      - npm run deploy
